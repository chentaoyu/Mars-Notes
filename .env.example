# ========================================
# Mars-Notes 环境变量配置示例
# ========================================
# 复制此文件为 .env 并修改相应配置
# cp .env.example .env

# ========================================
# 数据库配置
# ========================================
# 方式一：直接配置完整连接字符串（简单但不灵活）
# DATABASE_URL="postgresql://postgres:password@localhost:5432/notebook?schema=public"

# 方式二：使用变量引用（推荐，更灵活）
# Docker 环境中的数据库配置（基础变量）
POSTGRES_USER=notebook
POSTGRES_PASSWORD=notebook_password
POSTGRES_DB=notebook
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# PostgreSQL 连接字符串（使用变量引用自动组合）
# 格式: postgresql://用户名:密码@主机:端口/数据库名
# 语法: ${VAR_NAME} 可以引用上面定义的变量
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"

# ========================================
# NextAuth 配置
# ========================================
# NextAuth 完整 URL（生产环境需要修改为实际域名）
NEXTAUTH_URL=http://localhost:3000

# NextAuth 密钥（生产环境必须修改！）
# 生成方式: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production

# ========================================
# 应用配置
# ========================================
# 应用端口
APP_PORT=3000

# Node 环境
NODE_ENV=development

# ========================================
# 环境变量引用说明
# ========================================
# 项目已配置支持在 .env 文件中使用变量引用
# 
# 语法：
#   - 使用 ${VAR_NAME} 引用其他环境变量
#   - 变量引用会在加载时自动展开
#   - 支持嵌套引用（变量可以引用其他变量）
#   - 被引用的变量必须先定义
#
# 示例：
#   API_BASE_URL="https://api.example.com"
#   API_VERSION="v1"
#   API_URL="${API_BASE_URL}/${API_VERSION}"
#   # 结果：API_URL="https://api.example.com/v1"
#
# 数据库连接示例：
#   POSTGRES_USER="myuser"
#   POSTGRES_PASSWORD="mypass"
#   POSTGRES_HOST="localhost"
#   POSTGRES_PORT="5432"
#   POSTGRES_DB="mydb"
#   DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
